// Những kiến thức cơ bản của mảng
console.log("Bài học về mảng");
/**
 * Tạo mảng thì nó lưu vào những vùng nhớ khác nhau
 * Map là phương thức duyệt qua các phần tử ở trong mảng, nó sẽ tạo ta mảng mới không ảnh hưởng đến mảng góc
 * Tìm kiểu kĩ về hàm map() trong JS, nó sử dụng rất nhiều.
 * For each chỉ để đọc dữ liệu, chứ không dùng để ghi
 * Sort dùng để sắp xếp theo chuẩn UTF16
 * Spread operator dùng để clone mảng để không ảnh hưởng đến mảng gốc.
 */

const numbers = [1, 2, 3, 4, 5, 6, 7];

console.log(numbers[1]);
